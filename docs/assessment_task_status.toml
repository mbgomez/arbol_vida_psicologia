version = 1
project = "Numyah Mind"
updated_utc = "2026-03-25"
purpose = "Assessment continuation status for future product threads"

[current_state]
summary = "Assessments is now a real top-level tab, Home remains the fast-entry surface for start/resume/latest reflection value, History stays focused on completed sessions with a lightweight trend entry point plus a dedicated deeper-trends screen, Results has per-sephira detail plus an explicit retake path, Learn is present, Settings is present, assessment supports multi-sephira progression across the seeded set, the scoring engine now honors authored question weights, the project has a gated tester-observability foundation plus Firebase configuration and Play testing setup guidance, and the Phase 2 History polish slice is complete. Phase 2 overall remains unfinished, and the Phase 1 return slice for the section-complete content contract has now been implemented so section-complete reflection is routed through seeded JSON, repository mapping, and ViewModel selection instead of shared screen-level interpretation helpers."
status = "phase_2_unfinished_phase_1_content_contract_return_implemented"

[phase_0_status]
overall = "completed"
summary = "A lightweight `.codex` workflow layer is now locked for future threads so the project can keep the existing human docs as the source of truth while using a cheaper project map, task mini-agents, and reusable prompt templates for bounded-slice execution."
completed_items = [
  "Locked `.codex/project_map.md` as the low-token orientation file",
  "Added task mini-agents for inspect, implement, content contract, UI boundary, tests, verification, learnings, and next-slice generation",
  "Added reusable prompt templates under `.codex/prompts/`",
  "Locked that the `.codex` layer complements rather than replaces AGENTS, README, spec, roadmap, and status docs",
  "Locked one bounded slice per thread as the default execution pattern unless broader discovery is explicitly required"
]

[recent_work]
completed = [
  "Removed builder-style value-less home progress card",
  "Added Home summary driven by latest completed assessment",
  "Added last assessment date and relative days-since text on Home",
  "Separated Home resume state from latest-completed reflection state",
  "Promoted Assessments into the top-level shell and routed the library through that tab",
  "Kept Home focused on direct assessment continuation and latest-reflection value instead of library browsing",
  "Made shell exit-from-assessment confirmation work consistently when switching to any top-level tab",
  "Made shell exit dialog body and confirm action destination-aware instead of hardcoding Home",
  "Adjusted five-tab bottom-nav labeling to keep long labels readable on smaller phones without wrapping",
  "Shifted empty/error History actions toward the dedicated Assessments surface",
  "Added per-sephira results detail screen and navigation",
  "Added explicit fresh-start actions on Home, Assessments, and Results",
  "Added confirmation before replacing an unfinished in-progress assessment session",
  "Locked that replacing an unfinished session keeps completed History entries intact",
  "Added route-driven fresh-start assessment support instead of screen-local reset workarounds",
  "Expanded repository, route, ViewModel, and Compose coverage for the fresh-start flow",
  "Extended seeded assessment content shape with detailContent",
  "Added safe JSON fallbacks so unfinished seeded sections still load",
  "Locked the first History trend metrics and chart-ready state models while keeping the completed-session list primary",
  "Added a dedicated deeper-trends History screen reached from the lightweight History trend section",
  "Added explicit deeper-trends exploration modes for by sephira and by score type",
  "Kept deeper-trends chart state chart-ready and stable without adding repository-contract churn",
  "Added per-sephira visibility toggles in the score-type trends mode with immediate graph and detail-row updates",
  "Updated tests for Home, shell route ordering, the assessment library, Results detail, repository content mapping, and viewmodels",
  "Added a gated tester-observability foundation behind an app-owned telemetry interface",
  "Defined a small stable analytics taxonomy for onboarding, assessment entry, fresh-start confirmation, completion, results detail, history re-entry, learn reading, and key settings changes",
  "Added non-fatal reporting hooks for assessment, results, history, learn, and sephira-detail recoverable failures",
  "Added Gradle flags so tester observability and Firebase can be enabled only for tester builds",
  "Updated privacy-facing copy to explain limited tester telemetry and preserve the local-first trust boundary",
  "Added a dedicated tester-distribution and observability setup guide for Firebase and Google Play testing",
  "Updated telemetry-related Compose tests and confirmed that the broad `No compose hierarchies found` androidTest failure was caused by device state rather than production-code regressions",
  "Verified that Firebase backend initialization and local telemetry logging are active in a tester-enabled build",
  "Created a release upload keystore and produced a signed release app bundle for Play internal testing",
  "Updated the scoring engine so authored question weights now affect per-pole normalization and dominant-pole scoring",
  "Added repository and scoring tests to verify question weights survive content loading and influence score calculation",
  "Added real retry support to History so loading and recovery states can retry in place instead of only navigating away",
  "Polished History empty, loading, and error states with calmer guidance and clearer next-step copy",
  "Refined History trend cards and deeper-trends reading with clearer session context, latest-value cues, and calmer chart helper text",
  "Expanded History ViewModel and Compose coverage around retry and the new History-phase polish surfaces",
  "Adjusted support-list bullet alignment so multiline History guidance reads cleanly on device",
  "Added more breathing room under the full-interpretation Suggested practices heading",
  "Integrated 100, 50, and 0 labels into the graph axis so History trend charts read as one surface instead of separate text blocks",
  "Reworked shared placeholder/state surfaces so Learn, Results, and Sephira detail loading and recovery screens feel more intentional during nested-screen entry",
  "Tightened cross-surface production copy to remove roadmap-style and normative 'should' phrasing from onboarding, Assessments, Learn, and Settings",
  "Updated Learn Compose coverage to match the calmer single-surface locked-state presentation",
  "Confirmed a content-contract gap in the assessment section-complete screen: Yesod, Hod, and Netzach can inherit Malkuth-shaped interpretation copy when that screen relies on shared screen-level text mapping instead of sephira-authored completion content.",
  "Locked and implemented a dedicated completionContent seed/domain contract with sectionSummary plus pole-specific reflection and practice content",
  "Moved section-complete interpretation selection out of shared UI helpers and into repository/domain/ViewModel mapping",
  "Added repository fallback behavior so incomplete seeded sections can still build section-complete content from shortMeaning, detail patterns, and the first suggested practice",
  "Seeded authored completionContent for Malkuth, Yesod, Hod, and Netzach in the local questionnaire JSON",
  "Updated repository, ViewModel, and assessment screen tests for the new section-complete content contract",
  "Made seed JSON parsing backward-safe by defaulting optional authored fields such as suggestedPractices and nested completionContent values",
  "Confirmed that content-contract slices need fixture and local seed-constructor updates in the same pass to avoid Moshi parse failures and outdated test builders",
  "Locked a Phase 0 workflow layer with `.codex` mini-agents and prompt templates so future Codex threads can stay phase-aware and lower-token without weakening the existing project standards"
]

[locked_decisions]
home_top_level_tabs = ["Home", "Assessments", "History", "Learn", "Settings"]
assessment_library_top_level_tab = true
assessment_library_direction = "Assessments is now the dedicated top-level library surface. Home should still keep direct start/resume actions that route into the same assessment flow."
result_detail_content_shape = ["healthyExpression", "deficiencyPattern", "excessPattern", "suggestedPractices"]
result_detail_content_fallbacks = "If seeded sections omit detail fields, repository falls back to shortMeaning/introText/empty practices so the app remains stable while content is authored gradually."
history_behavior = "History remains the surface for completed sessions, opens the saved results overview for that exact session, and is the approved future home for graph-based trends."
history_trend_metrics = ["highest tension by saved session", "most settled by saved session"]
history_trend_navigation = "The lightweight History trend section remains the entry point, while deeper trend exploration now opens on a dedicated secondary History screen."
history_trend_modes = ["by sephira", "by score type"]
history_trend_filtering = "Score-type trend exploration can temporarily enable or disable individual sephirot in screen-local UI state without changing the saved-history trend contract."
debug_history_tooling = "Any future mock-history tooling should be debug-only and switch completed-history reads to sample data without mutating the real Room history."

[recommended_next_slice]
name = "phase_2_remaining_polish_after_content_contract_fix"
why = "The bounded Phase 1 return slice is now in place, so the next thread can safely resume remaining Phase 2 polish without carrying forward section-complete copy assumptions."

[[recommended_next_slice.goals]]
id = "manual_verify_completion_contract"
description = "Manually verify that Malkuth, Yesod, Hod, and Netzach now show their own authored section-complete reflection and practice content in the assessment flow."

[[recommended_next_slice.goals]]
id = "resume_phase_2_polish"
description = "Resume the remaining UX polish work now that the section-complete content pipeline is no longer screen-driven."

[phase_2_status]
overall = "in_progress"
completed_slices = ["history_polish", "copy_and_transition_polish"]
remaining_focus = [
  "visual readability polish outside History",
  "remaining richer empty, loading, and recovery states outside the History slice",
  "manual verification of the new section-complete content contract across seeded sephirot"
]
next_recommended_slice = "phase_2_remaining_polish_after_content_contract_fix"

[production_roadmap]
file = "docs/production_readiness_roadmap.md"
mainline_track = [
  "Phase 0: Workflow Foundation",
  "Phase 1: Product Completion",
  "Phase 1.5: Tester Distribution And Observability",
  "Phase 2: UX And Enjoyment Polish",
  "Phase 3: Refactor And Architecture Hardening",
  "Phase 4: Reliability And Coverage Expansion",
  "Phase 5: Accessibility And Localization Hardening",
  "Phase 6: Release Hardening",
  "Phase 7: Final QA And Submission Prep"
]
side_mission = "Sephira content enrichment can continue in parallel and be added one sephira at a time without blocking the mainline release track unless a feature explicitly depends on finalized content."

[future_options]
assessment_library_screen = "Now lives in the top-level Assessments tab while Home keeps direct start/resume/latest-results entry."
detail_content_backfill = "Backfill authored result-detail content for seeded sephirot still using fallback copy."
next_sephirot_batch = "Continue the mainline Phase 1 assessment expansion by locking and seeding the next contiguous batch after Netzach."
results_polish = "Strengthen bridges from Home summary into Results and detail after the explicit retake flow."
section_complete_content_contract = "Implemented as a bounded Phase 1 return slice: section-complete interpretation now uses a dedicated per-sephira JSON contract with repository/ViewModel mapping and safe fallbacks."
assessment_reentry_polish = "Review whether History or future assessment-library cards need additional calm wording around continuing versus starting fresh."
tester_distribution_and_observability = "A gated observability foundation is now in place, Firebase has been configured, and a signed release bundle has been created. The next step is to finish Analytics DebugView and Crashlytics delivery verification, complete Play account identity checks, and publish the internal testing release."
history_graphs = "Refine the new dedicated deeper-trends screen visually after the first mode and chart-state contracts settle through manual review."
history_phase_2_follow_up = "After the first History polish pass, review whether the new state copy and trend helper text feel equally calm in English and Spanish during manual device checks."
debug_history_switch = "Finish or refine a debug-only Settings control that explains its purpose and switches completed-history consumers to generated sample sessions without touching persisted user history."

[lessons_learned]
items = [
  "Do not tighten local JSON contracts without providing backward-safe defaults when seeded sections are still arriving gradually.",
  "Home should communicate user value, not repository or product-progress status.",
  "Home works better when in-progress continuation and latest-completed reflection are treated as separate surfaces rather than one blended summary card.",
  "A dedicated per-sephira detail route adds real product value and makes Results feel less like a terminal dashboard.",
  "Keeping result-detail content in the same seeded questionnaire pipeline is simpler than inventing a second local content system.",
  "The moment the product wants to signal a real multi-assessment future, including user-authored assessments, a top-level Assessments tab becomes a product statement rather than unnecessary shell noise.",
  "History is the right long-term home for graphs because it already owns completed-session re-entry and time-based reflection.",
  "The first History graph slice is stronger when it locks a small chart-ready metric contract instead of overreaching into dashboard behavior.",
  "The lightweight History trend section works best as the entry point, with deeper chart exploration moved onto a secondary screen instead of crowding the main saved-session surface.",
  "Explicit by-sephira and by-score-type modes keep the deeper trends experience easier to read than one overloaded graph control.",
  "Starting a fresh assessment is a trust-sensitive moment, so the app should confirm before replacing an unfinished session and should preserve completed History entries.",
  "Fresh-start behavior is safest when the route carries an explicit intent and the repository owns the replacement semantics, instead of each screen trying to reset assessment state on its own.",
  "Coverage for trust-sensitive flow changes is strongest when it spans repository behavior, route/use-case wiring, ViewModel state, and the visible confirmation path on production surfaces.",
  "Tester analytics are most useful when they stay narrowly focused on the core journey and key recoverable failures instead of trying to measure everything at once.",
  "Crash and telemetry instrumentation should respect the product's privacy posture and be explained clearly once tester-distribution work begins.",
  "A small app-owned telemetry layer keeps the event contract stable and lets Firebase be enabled only for tester builds instead of becoming a hidden requirement for every local build.",
  "When connected Compose tests all fail with `No compose hierarchies found`, check the device or emulator state first. In this thread, Android test-device settings were the cause rather than the new feature code.",
  "Firebase backend initialization logs and local Timber telemetry are not the same as end-to-end Analytics DebugView or Crashlytics console confirmation. Treat those as separate verification checkpoints.",
  "Play internal testing rollout can be blocked by account identity verification even after a signed release bundle is ready, so the release artifact and the console-compliance step should be tracked separately.",
  "For local Windows setup, invoking adb, keytool, and Gradle by absolute path can be faster than assuming PATH or JAVA_HOME are already configured.",
  "Per-sephira visibility toggles in score-type trend mode are a UI concern, so filtering the chart-ready screen model locally is cleaner than pushing transient selection state into repository contracts.",
  "Dynamic graph screens are easier to keep stable when test tags target specific controls and saved-point rows instead of relying on repeated sephira names in visible text.",
  "Five-tab bottom navigation needs explicit label-fit handling early; letting long labels wrap creates obvious UX debt on smaller phones.",
  "Assessment exit confirmation has to name the actual destination in both the body and the confirm action or the shell feels untrustworthy.",
  "Footer copy on production browsing surfaces should describe the user's experience, not the team's rollout structure.",
  "Once nested content screens become real product destinations, their loading, locked, and recovery states need the same visual tone as the rest of the app or the transition into detail feels abrupt.",
  "Small copy shifts away from 'should', 'will', and rollout language make trust-sensitive surfaces feel more present and less like staged product scaffolding.",
  "Section-complete assessment interpretation is part of the authored content contract, not just a UI copy problem. If it is not modeled per sephira in content, later sephirot will inherit accidental Malkuth assumptions.",
  "Debug/demo tools for completed-history review should switch sources instead of seeding or overwriting real saved data.",
  "Compose tests for long polished screens like History and Settings should scroll to below-the-fold content or rely on stable semantics instead of assuming those sections stay immediately visible.",
  "Scoring tests are more reliable when weighted fixtures stay comfortably away from confidence and dominant-pole thresholds unless the test is intentionally validating a boundary.",
  "Phase 2 polish on History is stronger when recovery states can retry in place instead of always pushing the user out to another tab.",
  "Trend visuals feel calmer when latest context, previous context, and readable point details stay visible together instead of asking the user to infer too much from the chart alone.",
  "For chart screens, y-axis labels such as 100, 50, and 0 should visually belong to the graph area rather than sit as detached text above or below it.",
  "Small support-list bullets on multiline cards need manual visual review; simple top or center alignment often looks wrong once the text wraps.",
  "Compose Canvas draw lambdas should receive plain values from composable scope. Looking up theme colors directly inside draw blocks can break compilation.",
  "When a section-complete reflection bug appears in one sephira, assume the content contract may be incomplete rather than treating it as an isolated copy fix. Lock the JSON/content shape before resuming broader UX polish.",
  "A repository fallback is not enough if the seed DTO still requires the field at parse time. Optional authored JSON fields need optional/defaulted DTO fields too.",
  "When a seeded content contract changes, update test JSON, local seed builders, and constructor call sites in the same pass or the next failure will often come from tests rather than production code.",
  "For cached questionnaire changes, manual verification should include a fresh install or cleared app data so the new content version and Room cache path are exercised together.",
  "A lightweight execution layer works best when it complements the human docs instead of replacing them. Keep product standards in AGENTS and the docs, and use `.codex` files only for cheap orientation and task routing.",
  "Token savings are strongest when future threads load only `AGENTS.md`, `.codex/project_map.md`, and the one or two mini-agents relevant to the slice.",
  "Reusable prompt templates should preserve existing project constraints instead of introducing a second conflicting rule set."
]

[verification]
status = "verified_tests_passed_manual_follow_up_pending"
notes = [
  "Unit tests passed after making detail JSON fields optional with repository fallbacks.",
  "Manual checks passed for Home summary and Results detail navigation.",
  "User verified the new top-level Assessments tab and assessment-exit shell behavior.",
  "User verified the destination-aware exit dialog copy and the footer-copy cleanup.",
  "User reported that all tests passed for the explicit fresh-start and retake flow.",
  "User reported that connected Compose tests passed after correcting the Android test-device state that was causing broad `No compose hierarchies found` failures.",
  "Firebase backend initialization was observed in Logcat and local telemetry events plus tester non-fatal logging were observed in a tester-enabled build.",
  "Analytics DebugView and Crashlytics console delivery were not yet confirmed end-to-end at the stopping point.",
  "A release upload keystore was created at `C:\\Users\\Miguel\\keys\\numyah-mind-upload.jks` and a signed release bundle was produced for Play internal testing.",
  "The Play upload path is prepared, but account identity verification still needs to be completed before fully finishing the internal testing rollout.",
  "The new History polish slice passed the targeted unit and instrumented tests.",
  "Manual checks also passed for History bullet alignment, Sephira detail Suggested practices spacing, and the integrated History graph axis labels.",
  "The targeted compile issue from a Canvas draw lambda was resolved by hoisting the theme color lookup out of the draw block.",
  "The cross-surface copy and transition polish slice has been implemented in code, but its updated Gradle and manual verification steps still need to be run by the user.",
  "The new section-complete content-contract slice has now passed the user's test runs after making the seed DTO backward-safe and fixing affected test builders.",
  "Manual fresh-install or clear-data verification of the new questionnaire version and section-complete authored copy across seeded sephirot is still recommended."
]

[files_to_read_first]
paths = [
  "AGENTS.md",
  ".codex/project_map.md",
  "README.md",
  "docs/product_spec.md",
  "docs/refactor_roadmap.md",
  "docs/assessment_task_status.toml"
]
